name: CI

on:
  pull_request:
    branches: 
      - master

jobs:
  build:

    runs-on: macos-latest

    steps:
    - uses: actions/ionic-deploymentApp@master
    - name: Install dependencies
      run: npm install
    - name: Test package
      run: ng test
    - name: Build package
      run: ng build
